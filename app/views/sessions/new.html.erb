<%#= form_for :session , method: "POST",:url => { :action => :create } do |form|%>
<%= form_for :session , url: session_path do |form| %>

  <%= form.label :email%>
  <%= form.text_field :email%>
  <%= form.submit "Sign in"%>
<%end%>
